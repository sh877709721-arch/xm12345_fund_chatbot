"""
意图分类到提示词的映射系统
根据意图分类结果返回相应的提示词
"""

from typing import Dict, Optional, List
from app.core.rule.intent import IntentClassifier, IntentResult
import logging

# 意图分类到提示词的映射字典
INTENT_PROMPT_MAPPING: Dict[str, str] = {
    "医疗服务项目目录": """用户正在咨询医疗服务项目目录相关内容。请提供准确的医疗服务项目信息，包括：
- 项目名称和编码
- 医保支付比例
- 就医医院等级要求
- 报销条件和限制
- 其他相关政策说明""",

    "医保退休办理": """用户正在咨询医保退休办理相关内容。请注意要点:
- 
    """,

    "个人账户一次性支取办理": """用户正在咨询个人账户一次性支取办理。请提供准确的办理指南：
- 支取条件和资格
- 申请材料和流程
- 支取金额计算方式
- 办理时限和注意事项
- 相关政策依据""",

    "辅助生殖医保支付政策": """用户正在咨询辅助生殖医保支付政策。请提供详细的政策解读：
- 可报销的辅助生殖项目
- 报销比例和限额
- 医院等级要求
- 申请条件和流程
- 政策实施时间和范围""",

    "药品目录": """用户正在咨询药品目录相关内容。请提供准确的药品信息：
- 药品名称和规格
- 医保支付类别
- 报销比例
- 使用限制
- 定点医院要求""",

    "连续参保机制": """用户正在咨询连续参保机制相关内容。请详细说明：
- 连续参保的优惠政策
- 断保补缴规定
- 待遇等待期计算
- 连续年限认定标准
- 相关政策影响""",

    "生育津贴待遇": """用户正在咨询生育津贴待遇。请提供准确的待遇信息：
- 津贴标准和计算方式
- 申请条件和材料
- 发放时间和方式
- 特殊情况处理
- 政策依据""",

    "惠厦保": """用户正在咨询惠厦保相关政策。请提供详细的保险信息：
- 保险责任范围
- 保费标准和缴费方式
- 理赔流程和条件
- 与基本医保的关系
- 参保条件和时限""",

    "社保": """用户正在咨询社保综合问题。请提供全面的社保信息：
- 五险一金包含内容
- 缴费比例和基数
- 待遇享受条件
- 转移接续规定
- 最新政策变化""",

    "转移接续手续办理": """用户正在咨询社保转移接续手续。请提供详细的办理指南：
- 转移条件和范围
- 办理流程和材料
- 办理时限和查询方式
- 注意事项和常见问题
- 各地政策差异""",
    "参保长效机制": """用户正在咨询参保长效机制。请详细说明：
- 长期参保的激励政策
- 待遇享受与缴费年限关系
- 累计计算方式
- 特殊情况处理
- 政策发展趋势""",

    "退费": """用户正在咨询退费相关业务。请提供准确的退费信息：
- 退费条件和范围
- 申请材料和流程
- 退费标准和时限
- 不同情况的退费规定
- 注意事项""",

    "医保账户划拨": """用户正在咨询医保账户划拨。请提供详细的划拨信息：
- 个人账户划拨比例
- 划拨时间和方式
- 划拨基数计算
- 不同群体的划拨差异
- 查询和异议处理""",

    "缴费标准": """用户正在咨询医保缴费标准。请提供准确的缴费信息：
- 缴费基数和比例
- 不同群体的缴费差异
- 缴费方式和时限
- 最低缴费年限
- 最新标准调整""",

    "医疗救助": """用户正在咨询医疗救助政策。请提供详细的救助信息：
- 救助对象和条件
- 救助标准和范围
- 申请流程和材料
- 救助方式和时限
- 与其他政策的关系""",

    "重复参保处理": """用户正在咨询重复参保处理。请提供准确的处理方案：
- 重复参保的认定标准
- 处理流程和方式
- 退费和转移规定
- 影响和注意事项
- 预防措施""",

    "家庭共济办理": """用户正在咨询家庭共济办理。请提供详细的办理指南：
- 家庭共济条件
- 绑定流程和方式
- 共济范围和限额
- 使用规则和注意事项
- 解除绑定方法""",

    "待遇生效时间": """用户正在咨询待遇生效时间。请提供准确的生效规则：
- 不同待遇的生效时限
- 特殊情况的生效规定
- 影响生效的因素
- 查询和确认方式
- 相关政策变化""",

    "生育津贴办理": """用户正在咨询生育津贴办理。请提供详细的办理流程：
- 申请条件和时间
- 所需材料和清单
- 办理方式和地点
- 审批时限和查询
- 常见问题解答""",

    "参保缴费方式": """用户正在咨询参保缴费方式。请提供全面的缴费信息：
- 缴费渠道和方式
- 线上线下操作流程
- 缴费时间和周期
- 代扣代缴规定
- 逾期处理""",

    "待遇标准": """用户正在咨询医保待遇标准。请提供准确的待遇信息：
- 各项待遇的具体标准
- 医院等级差异
- 报销比例和限额
- 特殊病种待遇
- 最新标准调整""",

    "参保缴费纠纷处理": """用户正在咨询参保缴费纠纷处理。请提供详细的解决方案：
- 常见纠纷类型
- 处理流程和部门
- 所需材料和证据
- 处理时限和结果
- 维权途径和建议""",

    "大病医保": """用户正在咨询大病医保政策。请提供详细的政策信息：
- 大病医保保障范围
- 报销标准和起付线
- 申请条件和流程
- 与基本医保的关系
- 最新政策变化""",

    "医疗费用报销办理": """用户正在咨询医疗费用报销办理。请提供准确的办理指南：
- 报销条件和范围
- 所需材料和清单
- 办理流程和时限
- 报销比例和限额
- 特殊情况处理""",

    "参保对象": """用户正在咨询参保对象相关内容。核心规则如下:
- 提问人和问答对象的社会关系，表达时不要出现多个**主语**,**重复论述**，例如“您妈妈作为厦门职工医保参保人员的非本市户籍父母”，“如果您妈妈是您（厦门职工医保参保人员）的非厦门户籍配偶” 此类的社会关系错误表达
- 可能需要根据实际情况分点论述:
   1)本地户籍和非本地户籍
   2)职工医保和非职工医保
""",
    "就医使用": """用户正在咨询医保就医使用。请提供详细的就医指导：
- 定点医院选择
- 就医流程和注意事项
- 医保卡使用方法
- 报销结算方式
- 异地就医规定""",
    "异地参保":"""用户正在咨询医保就医使用。请提供详细的就医指导
- 核心规则: 只可以解答厦门市的医保问题，异地参保人员医保报销规定请咨询参保地医保部门，引导咨询参保地12345咨询。
""",
    "其他": """

""",
}


def get_prompt_by_intent(intent_result: IntentResult) -> str:
    """
    根据意图分类结果获取对应的提示词

    Args:
        intent_result: 意图分类结果

    Returns:
        对应的提示词字符串
    """
    # 根据置信度选择最合适的意图
    intent = intent_result.intent

    # 如果置信度过低，使用"其他"类别的提示词
    if intent_result.confidence < 0.5:
        intent = "其他"

    # 获取提示词，如果没有找到则返回"其他"类别的提示词
    prompt = INTENT_PROMPT_MAPPING.get(intent, INTENT_PROMPT_MAPPING["其他"])

    # 添加置信度信息到提示词
    if intent_result.confidence >= 0.5:
        prompt = f"[意图识别: {intent} (置信度: {intent_result.confidence:.3f})]\n\n{prompt}"
    else:
        prompt = f"[意图识别: {intent} (置信度较低: {intent_result.confidence:.3f})]\n\n{prompt}"

    return prompt


def get_combined_prompt(intent_results: List[IntentResult], max_prompts: int = 2) -> str:
    """
    根据多个意图分类结果获取组合提示词

    Args:
        intent_results: 意图分类结果列表
        max_prompts: 最多组合的提示词数量

    Returns:
        组合的提示词字符串
    """
    if not intent_results:
        return INTENT_PROMPT_MAPPING["其他"]

    # 选择前几个置信度较高的结果
    top_results = intent_results[:max_prompts]

    if len(top_results) == 1:
        return get_prompt_by_intent(top_results[0])

    # 组合多个提示词
    prompts = []
    for i, result in enumerate(top_results):
        prompt = INTENT_PROMPT_MAPPING.get(result.intent, INTENT_PROMPT_MAPPING["其他"])
        prompts.append(f"[{i+1}] {result.intent} (置信度: {result.confidence:.3f})\n{prompt}")

    return "\n\n" + "="*50 + "\n\n".join(prompts)


def get_prompt_by_question(question: str, classifier: Optional[IntentClassifier] = None) -> str:
    """
    根据问题直接获取对应的提示词

    Args:
        question: 用户问题
        classifier: 意图分类器实例，如果不提供则创建新实例

    Returns:
        对应的提示词字符串
    """
    if classifier is None:
        classifier = IntentClassifier()

    # 进行意图分类
    results = classifier.classify(question)


    # 返回主要意图的提示词
    return get_prompt_by_intent(results[0]) if results else INTENT_PROMPT_MAPPING["其他"]

